{
  "name": "@hibro/node",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Hibro Node execution runtime",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node --experimental-strip-types src/cli.ts serve",
    "dev": "node --experimental-strip-types --watch src/cli.ts serve",
    "doctor": "node --experimental-strip-types src/cli.ts doctor",
    "run": "node --experimental-strip-types src/cli.ts run",
    "test": "node --experimental-strip-types scripts/test-runner.ts",
    "test:unit": "node --experimental-strip-types scripts/test-runner.ts --layer unit",
    "test:integration": "node --experimental-strip-types scripts/test-runner.ts --layer integration",
    "test:list": "node --experimental-strip-types scripts/test-runner.ts --list",
    "test:report": "node --experimental-strip-types scripts/test-runner.ts --junit",
    "test:e2e": "playwright test",
    "test:docker": "node --experimental-strip-types scripts/docker-smoke.ts",
    "check": "node --experimental-strip-types scripts/check.ts",
    "typecheck": "tsc --noEmit",
    "validate": "npm run typecheck && npm run check && npm test",
    "smoke:claude": "node --experimental-strip-types scripts/real-claude-smoke.ts",
    "release:package": "bash scripts/package-release.sh"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@types/node": "^26.1.1",
    "@types/ws": "^8.18.1",
    "typescript": "^7.0.2"
  }
}
